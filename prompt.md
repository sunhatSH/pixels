请帮我创建一个自动化部署脚本，完成以下完整流程：

### 需求描述

1. **Git 代码提交与推送**
   - 将本地修改的代码提交到 Git 仓库
   - 推送到远程仓库（重要：仓库地址是 `git@github.com:sunhaoSH/pixels.git`，不是 pixels 用户的仓库）

2. **AWS EC2 服务器操作**
   - 动态获取 EC2 实例的公网 IP 地址（实例可能经常关机重启，IP 会变化）
   - 通过 SSH 连接到 EC2 服务器
   - 在 EC2 上拉取最新的 Git 代码更新（确保使用正确的仓库地址：sunhaoSH 用户）
   - 在 EC2 上编译项目（Maven 编译）

3. **JAR 文件传输**
   - 从 EC2 下载编译好的 JAR 文件到本地 Mac
   - 上传 JAR 文件到 Lambda 专用的 S3 存储桶

4. **Lambda 函数部署**
   - 创建或更新 AWS Lambda 函数（如果不存在则创建，存在则更新代码）
   - 使用 S3 中的 JAR 文件作为函数代码

5. **测试验证**
   - 调用 Lambda 函数执行测试
   - 验证部署是否成功

### 交付要求

请按以下顺序完成：

1. **文档**：创建一份 Markdown 文档，详细记录整个流程的步骤、配置参数、注意事项和故障排除方法

2. **脚本**：编写一个完整的自动化脚本（Shell/Bash），实现上述所有步骤，包含：
   - 错误处理机制
   - 日志输出
   - 参数配置（实例 ID、S3 桶名、Lambda 函数名等）
   - 动态 IP 获取逻辑
   - Git 仓库地址验证

3. **执行与调试**：执行脚本一次，记录所有输出，如有错误则进行调试和修复

### 注意事项

- EC2 实例 ID: `i-0e01b0d7947291b0b`
- AWS 区域: `us-east-1` (EC2) / `us-east-2` (Lambda)
- Git 仓库: `git@github.com:sunhaoSH/pixels.git`（注意用户名）
- 确保不会拉错 Git 仓库地址
- 配置文件不要上传到 Git（已在 .gitignore 中）
- 需要处理实例可能处于停止状态的情况（自动启动）

### 输出格式

请创建以下文件：
- `DEPLOYMENT_AUTOMATION_GUIDE.md` - 完整流程文档
- `auto-deploy.sh` - 自动化部署脚本
- 执行输出和调试日志